import { Eye } from "lucide-react";
import type { Expense } from "../../../../services/expenseService";
export default function ExpensesTable({ expenses, loading, onView }: { expenses: Expense[]; loading: boolean; onView: (expense: Expense) => void }) { return <div className="w-full min-w-0 overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="max-w-full overflow-x-auto"><table className="w-max min-w-full text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr>{["Date", "Category", "Payee", "Type", "Method", "Reference", "Amount", "Status", "Actions"].map((item) => <th key={item} className="whitespace-nowrap px-5 py-4 font-semibold">{item}</th>)}</tr></thead><tbody className="divide-y divide-slate-200">{loading ? <tr><td colSpan={9} className="px-5 py-10 text-center text-slate-500">Loading expenses...</td></tr> : expenses.length === 0 ? <tr><td colSpan={9} className="px-5 py-10 text-center text-slate-500">No expenses match these filters.</td></tr> : expenses.map((expense) => <tr key={expense.id}><td className="whitespace-nowrap px-5 py-4">{new Date(expense.expenseDate).toLocaleDateString()}</td><td className="px-5 py-4">{expense.category}</td><td className="whitespace-nowrap px-5 py-4">{expense.payeeName}</td><td className="px-5 py-4">{expense.payeeType}</td><td className="px-5 py-4">{expense.method.replaceAll("_", " ")}</td><td className="px-5 py-4">{expense.referenceNumber ?? "—"}</td><td className="whitespace-nowrap px-5 py-4 font-semibold">UGX {expense.amount.toLocaleString()}</td><td className="px-5 py-4"><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold">{expense.status.replaceAll("_", " ")}</span></td><td className="px-5 py-4"><button type="button" title="View expense" onClick={() => onView(expense)} className="rounded-lg p-2 text-blue-700 hover:bg-blue-50"><Eye size={18}/></button></td></tr>)}</tbody></table></div></div>; }
