import type { ExpenseFilters as Filters, ExpenseTeacher } from "../../../../services/expenseService";
const categories = ["Teacher Payments", "Food Supplies", "Electricity", "Water", "Internet", "Rent", "Repairs", "School Materials", "Transport", "Examination Costs", "Medical Expenses", "Events", "Construction", "Office Expenses", "Other"];
export default function ExpenseFilters({ value, teachers, onChange }: { value: Filters; teachers: ExpenseTeacher[]; onChange: (value: Filters) => void }) { const set = (key: keyof Filters, next: string) => onChange({ ...value, [key]: next || undefined, page: 1 }); const field = "rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm"; return <div className="grid gap-3 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:grid-cols-2 lg:grid-cols-4"><input type="search" value={value.search ?? ""} onChange={(event) => set("search", event.target.value)} placeholder="Payee, category, or reference" className={`${field} sm:col-span-2`}/><input type="date" value={value.startDate ?? ""} onChange={(event) => set("startDate", event.target.value)} className={field}/><input type="date" value={value.endDate ?? ""} onChange={(event) => set("endDate", event.target.value)} className={field}/><select value={value.category ?? ""} onChange={(event) => set("category", event.target.value)} className={field}><option value="">All categories</option>{categories.map((item) => <option key={item}>{item}</option>)}</select><select value={value.payeeType ?? ""} onChange={(event) => set("payeeType", event.target.value)} className={field}><option value="">All payee types</option><option>SUPPLIER</option><option>TEACHER</option><option>OTHER</option></select><select value={value.teacherId ?? ""} onChange={(event) => set("teacherId", event.target.value)} className={field}><option value="">All teachers</option>{teachers.map((teacher) => <option key={teacher.id} value={teacher.id}>{teacher.firstName} {teacher.lastName}</option>)}</select><select value={value.status ?? ""} onChange={(event) => set("status", event.target.value)} className={field}><option value="">All statuses</option>{["PENDING_APPROVAL", "APPROVED", "PAID", "REJECTED", "CANCELLED"].map((item) => <option key={item}>{item.replaceAll("_", " ")}</option>)}</select></div>; }
