import { useState } from "react";
export default function ReversePaymentModal({ onConfirm, onCancel, loading }: { onConfirm: (reason: string) => Promise<void>; onCancel: () => void; loading: boolean }) { const [reason, setReason] = useState(""); return <div className="rounded-2xl border border-rose-200 bg-rose-50 p-4"><h4 className="font-semibold text-rose-900">Reverse payment</h4><p className="mt-1 text-sm text-rose-700">This preserves the payment and creates an audit record.</p><textarea value={reason} onChange={(event) => setReason(event.target.value)} className="mt-3 w-full rounded-xl border border-rose-200 bg-white p-3 text-sm" placeholder="Reason for reversal"/><div className="mt-3 flex gap-3"><button type="button" disabled={!reason.trim() || loading} onClick={() => void onConfirm(reason)} className="rounded-xl bg-rose-700 px-4 py-2 text-sm font-semibold text-white disabled:bg-rose-300">{loading ? "Reversing..." : "Confirm reversal"}</button><button type="button" onClick={onCancel} className="rounded-xl border border-rose-200 px-4 py-2 text-sm font-semibold text-rose-700">Cancel</button></div></div>; }
