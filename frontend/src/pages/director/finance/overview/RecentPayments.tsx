import { Link } from "react-router-dom";
import type { FinanceSummary } from "../../../../services/financeOverviewService";
import { formatKampalaDateTime } from "../../../../utils/kampalaDateTime";

export default function RecentPayments({ payments }: { payments: FinanceSummary["recentPayments"] }) {
  return <section className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between p-6"><div><h3 className="font-semibold text-slate-900">Recent payments</h3><p className="mt-1 text-sm text-slate-500">Latest received student payments</p></div><Link to="?tab=payments" className="text-sm font-medium text-blue-600 hover:underline">View all</Link></div><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-6 py-3">Student</th><th className="px-6 py-3">Receipt</th><th className="px-6 py-3">Date</th><th className="px-6 py-3 text-right">Amount</th></tr></thead><tbody className="divide-y divide-slate-100">{payments.length ? payments.map((payment) => <tr key={payment.id}><td className="px-6 py-3"><p className="font-medium text-slate-800">{payment.student ? `${payment.student.firstName} ${payment.student.lastName}` : "Student"}</p><p className="text-xs text-slate-500">{payment.student?.admissionNumber}</p></td><td className="px-6 py-3 text-slate-600">{payment.receiptNumber || "—"}</td><td className="px-6 py-3 text-slate-600">{formatKampalaDateTime(payment.date)}</td><td className="px-6 py-3 text-right font-semibold">UGX {payment.amount.toLocaleString()}</td></tr>) : <tr><td colSpan={4} className="px-6 py-8 text-center text-slate-500">No recent payments.</td></tr>}</tbody></table></div></section>;
}
