import type { FinanceSummary } from "../../../../services/financeOverviewService";
export default function CollectionProgress({ summary }: { summary: FinanceSummary }) { const percentage = Math.min(100, summary.collectionPercentage || 0); return <div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-end justify-between gap-4"><div><p className="font-semibold text-slate-900">Fee collection progress</p><p className="mt-1 text-sm text-slate-500">{percentage}% of charged fees collected</p></div><p className="text-2xl font-bold text-emerald-600">{percentage}%</p></div><div className="mt-4 h-3 overflow-hidden rounded-full bg-slate-100"><div style={{ width: `${percentage}%` }} className="h-full rounded-full bg-emerald-500"/></div></div>; }
