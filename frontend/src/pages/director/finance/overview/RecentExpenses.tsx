import { Link } from "react-router-dom";
import type { FinanceSummary } from "../../../../services/financeOverviewService";

export default function RecentExpenses({ expenses }: { expenses: FinanceSummary["recentExpenses"] }) {
  return <section className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between p-6"><div><h3 className="font-semibold text-slate-900">Recent expenses</h3><p className="mt-1 text-sm text-slate-500">Latest paid school expenses</p></div><Link to="?tab=expenses" className="text-sm font-medium text-blue-600 hover:underline">View all</Link></div><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-6 py-3">Payee</th><th className="px-6 py-3">Category</th><th className="px-6 py-3">Date</th><th className="px-6 py-3 text-right">Amount</th></tr></thead><tbody className="divide-y divide-slate-100">{expenses.length ? expenses.map((expense) => <tr key={expense.id}><td className="px-6 py-3 font-medium text-slate-800">{expense.payeeName}</td><td className="px-6 py-3 text-slate-600">{expense.category.replaceAll("_", " ")}</td><td className="px-6 py-3 text-slate-600">{new Date(expense.expenseDate).toLocaleString()}</td><td className="px-6 py-3 text-right font-semibold">UGX {expense.amount.toLocaleString()}</td></tr>) : <tr><td colSpan={4} className="px-6 py-8 text-center text-slate-500">No recent expenses.</td></tr>}</tbody></table></div></section>;
}
