import type { FinanceReportData } from "../../../../services/financeReportService";
export default function FeeCollectionReport({ data }: { data: FinanceReportData }) { const expected = data.accounts.reduce((sum, item) => sum + item.expectedAmount, 0); const paid = data.accounts.reduce((sum, item) => sum + item.paidAmount, 0); return <div className="grid gap-4 sm:grid-cols-3">{[["Expected", expected], ["Collected", paid], ["Balance", expected - paid]].map(([label, value]) => <div key={String(label)} className="rounded-2xl border border-slate-200 p-4"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-xl font-semibold">UGX {Number(value).toLocaleString()}</p></div>)}</div>; }
