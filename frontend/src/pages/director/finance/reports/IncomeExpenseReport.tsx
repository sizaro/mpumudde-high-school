import type { FinanceReportData } from "../../../../services/financeReportService";
export default function IncomeExpenseReport({ data }: { data: FinanceReportData }) { const income = data.payments.filter((item) => item.status === "COMPLETED").reduce((sum, item) => sum + item.amount, 0); const expense = data.expenses.filter((item) => item.status === "PAID").reduce((sum, item) => sum + item.amount, 0); return <div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl bg-emerald-50 p-4">Income: UGX {income.toLocaleString()}</div><div className="rounded-2xl bg-rose-50 p-4">Expenses: UGX {expense.toLocaleString()}</div><div className="rounded-2xl bg-slate-100 p-4">Net: UGX {(income - expense).toLocaleString()}</div></div>; }
