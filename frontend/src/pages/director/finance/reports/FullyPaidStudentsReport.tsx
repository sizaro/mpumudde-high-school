import type { FinanceReportData } from "../../../../services/financeReportService";
export default function FullyPaidStudentsReport({ data }: { data: FinanceReportData }) { const rows = data.accounts.filter((item) => item.status === "FULLY_PAID" || item.status === "WAIVED"); return <div className="space-y-2">{rows.length ? rows.map((item) => <div key={item.id} className="flex flex-wrap justify-between gap-3 rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-800"><span><b>{item.firstName} {item.lastName}</b> — {item.admissionNumber}</span><span>{item.schoolClass?.name ?? "No class"} · UGX {item.paidAmount.toLocaleString()} paid</span></div>) : <p className="text-sm text-slate-500">No fully paid accounts match these filters.</p>}</div>; }
