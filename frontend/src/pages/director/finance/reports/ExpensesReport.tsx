import type { FinanceReportData } from "../../../../services/financeReportService";
export default function ExpensesReport({ data }: { data: FinanceReportData }) { const paid = data.expenses.filter((item) => item.status === "PAID").reduce((sum, item) => sum + item.amount, 0); return <div><p className="mb-4 text-sm text-slate-600">Paid expenses: <b>UGX {paid.toLocaleString()}</b></p><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Payee</th><th className="px-4 py-3">Category</th><th className="px-4 py-3">Date</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Recorded by</th><th className="px-4 py-3 text-right">Amount</th></tr></thead><tbody className="divide-y divide-slate-100">{data.expenses.length ? data.expenses.map((item) => <tr key={item.id}><td className="px-4 py-3 font-medium">{item.payeeName}</td><td className="px-4 py-3">{item.category.replaceAll("_", " ")}</td><td className="px-4 py-3">{new Date(item.expenseDate).toLocaleString()}</td><td className="px-4 py-3">{item.status}</td><td className="px-4 py-3">{item.createdBy?.email ?? "—"}</td><td className="px-4 py-3 text-right font-semibold">UGX {item.amount.toLocaleString()}</td></tr>) : <tr><td colSpan={6} className="px-4 py-8 text-center text-slate-500">No expenses match these filters.</td></tr>}</tbody></table></div></div>; }
