import type { FinanceReportData } from "../../../../services/financeReportService";
export default function OutstandingBalancesReport({ data }: { data: FinanceReportData }) { const rows = data.accounts.filter((item) => item.outstandingBalance > 0); const total = rows.reduce((sum, item) => sum + item.outstandingBalance, 0); return <div><p className="mb-4 text-sm text-slate-600">Total outstanding: <b>UGX {total.toLocaleString()}</b></p><div className="space-y-2">{rows.length ? rows.map((item) => <div key={item.id} className="flex flex-wrap justify-between gap-3 rounded-xl border border-slate-200 p-3 text-sm"><span><b>{item.firstName} {item.lastName}</b> — {item.admissionNumber}<span className="ml-2 text-slate-500">{item.schoolClass?.name}</span></span><strong className="text-rose-700">UGX {item.outstandingBalance.toLocaleString()}</strong></div>) : <p className="text-sm text-slate-500">No outstanding balances.</p>}</div></div>; }
