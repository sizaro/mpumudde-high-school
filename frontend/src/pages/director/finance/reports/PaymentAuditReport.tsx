import type { FinanceReportData } from "../../../../services/financeReportService";
export default function PaymentAuditReport({ data }: { data: FinanceReportData }) { const entries = data.payments.flatMap((payment) => payment.audits?.length ? payment.audits.map((audit) => ({ ...audit, payment })) : [{ id: payment.id, action: payment.status, createdAt: payment.createdAt, reason: payment.reversalReason, actor: payment.recordedBy, payment }]); return <div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Date</th><th className="px-4 py-3">Receipt</th><th className="px-4 py-3">Action</th><th className="px-4 py-3">Performed by</th><th className="px-4 py-3">Reason / change</th></tr></thead><tbody className="divide-y divide-slate-100">{entries.length ? entries.sort((a, b) => new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime()).map((entry) => <tr key={`${entry.payment.id}-${entry.id}`}><td className="px-4 py-3">{new Date(entry.createdAt).toLocaleString()}</td><td className="px-4 py-3 font-medium">{entry.payment.receiptNumber ?? entry.payment.id}</td><td className="px-4 py-3">{entry.action}</td><td className="px-4 py-3">{entry.actor?.email ?? "System"}</td><td className="max-w-sm px-4 py-3 text-slate-600">{entry.reason || (entry.changes ? JSON.stringify(entry.changes) : "—")}</td></tr>) : <tr><td colSpan={5} className="px-4 py-8 text-center text-slate-500">No audit activity matches these filters.</td></tr>}</tbody></table></div>; }
