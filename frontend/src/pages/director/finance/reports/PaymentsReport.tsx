import type { FinanceReportData } from "../../../../services/financeReportService";
import { formatKampalaDateTime } from "../../../../utils/kampalaDateTime";
export default function PaymentsReport({ data }: { data: FinanceReportData }) { const total = data.payments.filter((item) => ["COMPLETED", "completed"].includes(item.status)).reduce((sum, item) => sum + item.amount, 0); return <div><p className="mb-4 text-sm text-slate-600">Valid collections: <b>UGX {total.toLocaleString()}</b></p><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Receipt</th><th className="px-4 py-3">Student</th><th className="px-4 py-3">Date</th><th className="px-4 py-3">Method</th><th className="px-4 py-3">Status</th><th className="px-4 py-3 text-right">Amount</th></tr></thead><tbody className="divide-y divide-slate-100">{data.payments.length ? data.payments.map((item) => <tr key={item.id}><td className="px-4 py-3 font-medium">{item.receiptNumber ?? "Legacy payment"}</td><td className="px-4 py-3">{item.student?.firstName} {item.student?.lastName}<span className="block text-xs text-slate-500">{item.student?.admissionNumber}</span></td><td className="px-4 py-3">{formatKampalaDateTime(item.date)}</td><td className="px-4 py-3">{item.method?.replaceAll("_", " ")}</td><td className="px-4 py-3">{item.status}</td><td className="px-4 py-3 text-right font-semibold">UGX {item.amount.toLocaleString()}</td></tr>) : <tr><td colSpan={6} className="px-4 py-8 text-center text-slate-500">No payments match these filters.</td></tr>}</tbody></table></div></div>; }
