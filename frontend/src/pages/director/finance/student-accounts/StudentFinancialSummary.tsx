import type { StudentAccountDetails } from "../../../../services/studentAccountService";
export default function StudentFinancialSummary({ account }: { account: StudentAccountDetails }) { const cards = [{ label: "Expected fees", value: account.summary.expectedAmount }, { label: "Paid", value: account.summary.paidAmount }, { label: "Waivers", value: account.summary.waivedAmount }, { label: "Previous balance", value: account.summary.previousBalance }, { label: "Outstanding", value: account.summary.outstandingBalance }]; return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{cards.map((card) => <div key={card.label} className="rounded-2xl border border-slate-200 bg-slate-50 p-4"><p className="text-xs font-medium uppercase tracking-wide text-slate-500">{card.label}</p><p className="mt-2 text-xl font-semibold text-slate-900">UGX {card.value.toLocaleString()}</p></div>)}</div>; }
