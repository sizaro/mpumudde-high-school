import type { StudentAccount } from "../../../../services/studentAccountService";
type Props = { accounts: StudentAccount[]; loading: boolean; onOpen: (account: StudentAccount) => void };
const money = (amount: number) => `UGX ${amount.toLocaleString()}`;
export default function StudentAccountsTable({ accounts, loading, onOpen }: Props) { return <div className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200 text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr>{["Student", "Class", "Category", "Expected", "Paid", "Balance", "Status", ""].map((heading) => <th key={heading} className="px-5 py-4 font-semibold">{heading}</th>)}</tr></thead><tbody className="divide-y divide-slate-200">{loading ? <tr><td colSpan={8} className="px-5 py-8 text-center text-slate-500">Loading student accounts...</td></tr> : accounts.length === 0 ? <tr><td colSpan={8} className="px-5 py-8 text-center text-slate-500">No student accounts match these filters.</td></tr> : accounts.map((account) => <tr key={account.id} className="hover:bg-slate-50"><td className="px-5 py-4"><p className="font-medium text-slate-900">{account.firstName} {account.lastName}</p><p className="text-xs text-slate-500">{account.admissionNumber}</p></td><td className="px-5 py-4 text-slate-600">{account.schoolClass?.name ?? "—"}</td><td className="px-5 py-4 text-slate-600">{account.studentCategory?.name ?? "—"}</td><td className="px-5 py-4">{money(account.expectedAmount)}</td><td className="px-5 py-4">{money(account.paidAmount)}</td><td className="px-5 py-4 font-semibold text-rose-700">{money(account.outstandingBalance)}</td><td className="px-5 py-4"><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-700">{account.status.replaceAll("_", " ")}</span></td><td className="px-5 py-4"><button type="button" onClick={() => onOpen(account)} className="rounded-xl border border-slate-200 px-3 py-2 font-medium text-slate-700 hover:bg-slate-50">View</button></td></tr>)}</tbody></table></div></div>; }
