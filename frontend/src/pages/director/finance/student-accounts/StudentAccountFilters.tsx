import type { AccountStatus, StudentAccountFilters as Filters } from "../../../../services/studentAccountService";

type Props = { value: Filters; onChange: (value: Filters) => void };
const inputClass = "mt-2 w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm outline-none focus:border-slate-400";
const statuses: Array<{ value: AccountStatus; label: string }> = [{ value: "FULLY_PAID", label: "Fully Paid" }, { value: "PARTIALLY_PAID", label: "Partially Paid" }, { value: "NOT_PAID", label: "Not Paid" }, { value: "OVERPAID", label: "Overpaid" }, { value: "WAIVED", label: "Waived" }];
export default function StudentAccountFilters({ value, onChange }: Props) { return <div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="grid gap-4 md:grid-cols-2"><label className="text-sm font-medium text-slate-700">Student search<input type="search" className={inputClass} value={value.search ?? ""} onChange={(event) => onChange({ ...value, search: event.target.value })} placeholder="Name or admission number"/></label><label className="text-sm font-medium text-slate-700">Payment status<select className={inputClass} value={value.status ?? ""} onChange={(event) => onChange({ ...value, status: event.target.value as AccountStatus | undefined })}><option value="">All statuses</option>{statuses.map((status) => <option key={status.value} value={status.value}>{status.label}</option>)}</select></label></div></div>; }
