import type { StudentAccountDetails } from "../../../../services/studentAccountService";
export default function PreviousBalances({ balances }: { balances: StudentAccountDetails["previousBalances"] }) { const total = balances.reduce((sum, item) => sum + item.balance, 0); return <div className="overflow-hidden rounded-2xl border border-slate-200">{balances.length ? <table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr><th className="px-4 py-3">Period</th><th className="px-4 py-3">Fee</th><th className="px-4 py-3">Expected</th><th className="px-4 py-3">Paid / waived</th><th className="px-4 py-3 text-right">Brought forward</th></tr></thead><tbody className="divide-y divide-slate-100">{balances.map((item) => <tr key={item.id}><td className="px-4 py-3">{item.academicYear} · {item.term}</td><td className="px-4 py-3">{item.feeType}</td><td className="px-4 py-3">UGX {item.expectedAmount.toLocaleString()}</td><td className="px-4 py-3">UGX {(item.paidAmount + item.waivedAmount).toLocaleString()}</td><td className="px-4 py-3 text-right font-semibold text-rose-700">UGX {item.balance.toLocaleString()}</td></tr>)}</tbody><tfoot className="bg-slate-50"><tr><td colSpan={4} className="px-4 py-3 font-semibold">Total previous balance</td><td className="px-4 py-3 text-right font-bold">UGX {total.toLocaleString()}</td></tr></tfoot></table> : <p className="p-4 text-sm text-emerald-700">No unpaid balances from a previous term.</p>}</div>; }
