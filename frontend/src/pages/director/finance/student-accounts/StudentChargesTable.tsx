import type { StudentAccountDetails } from "../../../../services/studentAccountService";
export default function StudentChargesTable({ charges }: { charges: StudentAccountDetails["charges"] }) { return <div className="overflow-hidden rounded-2xl border border-slate-200"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr><th className="px-4 py-3">Fee</th><th className="px-4 py-3">Expected</th><th className="px-4 py-3">Paid</th><th className="px-4 py-3">Balance</th></tr></thead><tbody className="divide-y divide-slate-200">{charges.map((charge) => <tr key={charge.id}><td className="px-4 py-3 text-slate-800">{charge.financeStructure.feeType.name}<span className="block text-xs text-slate-500">{charge.financeStructure.term.name}</span></td><td className="px-4 py-3">{charge.expectedAmount.toLocaleString()}</td><td className="px-4 py-3">{charge.paidAmount.toLocaleString()}</td><td className="px-4 py-3 font-semibold">{(charge.expectedAmount - charge.paidAmount - charge.waivedAmount).toLocaleString()}</td></tr>)}</tbody></table></div>; }
