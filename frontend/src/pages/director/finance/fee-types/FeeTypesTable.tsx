import { Pencil, Power, Trash2 } from "lucide-react";
import type { FinanceFeeType } from "../../../../services/feeTypeService";

export default function FeeTypesTable({ items, loading, canManage, onEdit, onToggle, onDelete }: { items: FinanceFeeType[]; loading: boolean; canManage: boolean; onEdit: (item: FinanceFeeType) => void; onToggle: (item: FinanceFeeType) => void; onDelete: (item: FinanceFeeType) => void }) {
  return <div className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr><th className="px-5 py-4">Fee type</th><th className="px-5 py-4">Structures</th><th className="px-5 py-4">Payments</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Actions</th></tr></thead><tbody className="divide-y divide-slate-200">{loading ? <tr><td colSpan={5} className="px-5 py-10 text-center text-slate-500">Loading fee types...</td></tr> : items.length === 0 ? <tr><td colSpan={5} className="px-5 py-10 text-center text-slate-500">No fee types found.</td></tr> : items.map((item) => { const unused = item._count.financeStructures === 0 && item._count.payments === 0; return <tr key={item.id}><td className="px-5 py-4 font-semibold text-slate-900">{item.name}</td><td className="px-5 py-4">{item._count.financeStructures}</td><td className="px-5 py-4">{item._count.payments}</td><td className="px-5 py-4"><span className={`rounded-full px-3 py-1 text-xs font-semibold ${item.isActive ? "bg-emerald-100 text-emerald-700" : "bg-slate-100 text-slate-600"}`}>{item.isActive ? "ACTIVE" : "INACTIVE"}</span></td><td className="px-5 py-4"><div className="flex gap-1">{canManage && <><button type="button" title="Edit fee type" onClick={() => onEdit(item)} className="rounded-lg p-2 text-amber-700 hover:bg-amber-50"><Pencil size={17}/></button><button type="button" title={item.isActive ? "Deactivate" : "Activate"} onClick={() => onToggle(item)} className="rounded-lg p-2 text-blue-700 hover:bg-blue-50"><Power size={17}/></button>{unused && <button type="button" title="Delete unused fee type" onClick={() => onDelete(item)} className="rounded-lg p-2 text-rose-700 hover:bg-rose-50"><Trash2 size={17}/></button>}</>}</div></td></tr>; })}</tbody></table></div></div>;
}
